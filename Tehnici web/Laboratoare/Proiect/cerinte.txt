let(necesar),    (e facut ish)

getComputedStyle

event -proprietati(  
      key/pageX)

setInterval(cu >= 3 parametrii-necesar) ,  (nuj :) )
clearInterval



Sign up
      user name
      email, 
      parola de doua ori
      data nastere

Cand fac log in salvez userul in local storage si cand dau submit la form iau userul de acolo :)


